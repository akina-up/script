```pip install soundfile numpy scipy matplotlib rich```

将文件/文件夹拖放到audio.bat会自动生成waveforms,spectrograms,bit_depth,plotfreq图和amplitude statisticstxt文件(Dynamics还需要手动从foobar2000的DR Meter复制)

将文件/文件夹拖放到check_mono.bat，会自动反相音轨叠加生成频谱图以方便检查是否是dual mono