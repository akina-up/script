# script

## qui
```'bot token' '频道ID' "{name}" "{progress}"```



## autobrr

```'bot token1;bot token2' '频道ID1;频道ID2' '#自定义' "{{.IndexerName}}" "{{.TorrentName}}" '{{.Size}}' '{{.InfoUrl}}' '{{.Uploader}}'```

多个频道或bot token用 ; 分割
